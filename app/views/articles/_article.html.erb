<div id="<%= dom_id article %>">
  <p>
    <strong>Title:</strong>
    <%= article.title %>
  </p>

  <p>
    <strong>Content:</strong>
    <%= article.content %>
  </p>

    <strong>Tags:</strong>
    <% article.tags.each do |tag| %>
      <p><%= tag.tag_name %></p>
    <% end %>

</div>
